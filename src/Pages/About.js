import React from 'react'
import KeyFeatures from '../Components/KeyFeatures'

const About = ({mode}) => {
  return (
    <div>
      <KeyFeatures heading="About Us" mode={mode} />
    </div>
  )
}

export default About
